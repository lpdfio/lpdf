[package]
name = "lpdf-cli"
version = "0.0.0"
edition = "2024"
description = "lpdf — CLI tool (convert, codegen, benchmark)"
repository = "https://github.com/lpdfio/lpdf"
license-file = "../core/LICENSE"

[[bin]]
name = "lpdf"
path = "src/main.rs"

[dependencies]
lpdf = { path = "../core" }
